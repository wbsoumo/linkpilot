# Custom Rules

- **Do Not Use Live Browser**: Under no circumstances should you spawn a browser subagent or use any live browser tools (such as `browser_subagent`, `open_browser_url`, `read_browser_page`, etc.) to verify layouts or pages. Do all layout and structural verification through code analysis and code reviews.
